using MakeGrid;
using MakeGrid.Game;

class Program
{
    static void Main()
    {
        //GameController game = new GameController();
        //game.Run();
        Grid grid = new Grid();
        grid.DrawGrid();
    }
}